import { ArsCurrencyPipe } from './ars-currency.pipe';

describe('ArsCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ArsCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
